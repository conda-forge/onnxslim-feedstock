{% set name = "onnxslim" %}
{% set version = "0.1.71" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: dfabbdce2baa7bc721668575c5e58240144e40fcd951702a6d0b4fdfcf3b765d

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - onnxslim = onnxslim.cli:main

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - colorama
    - ml_dtypes
    - onnx
    - packaging
    - python >={{ python_min }}
    - sympy >=1.13.3

test:
  imports:
    - onnxslim
  commands:
    - pip check
    - onnxslim --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/inisis/OnnxSlim
  summary: A Toolkit to Help Optimize Onnx Model
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jeongseok-meta
